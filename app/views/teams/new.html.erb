<div class="wrapper">
  <div class="create_new_team-form">
    <%= simple_form_for @team do |f| %>
      <%= f.input :title, label: false, placeholder: "Название команды" %>
      <%= f.input :description, label: false, placeholder: "Краткое опиание", input_html: {rows: 5} %>
      <%= f.input :owner_id, as: :hidden, input_html:{value: current_profile.id} %>
      <%= f.submit "Создать" %>
    <% end %>
  </div>
</div>
